{
  "info": {
    "name": "TrailBlaze Mock API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health",
      "request": {
        "method": "GET",
        "url": {"raw": "{{base_url}}/health", "host": ["{{base_url}}"], "path": ["health"]}
      }
    },
    {
      "name": "Chat Mock",
      "request": {
        "method": "GET",
        "url": {"raw": "{{base_url}}/api/v1/chat/mock", "host": ["{{base_url}}"], "path": ["api", "v1", "chat", "mock"]}
      }
    }
  ]
}
